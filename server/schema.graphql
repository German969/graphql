# A small blog API: read blog info and posts, publish new posts.

type Post {
  id: ID!
  title: String!
  body: String!
  publishedAt: String!
}

type Query {
  blogName: String
  serverTime: String
  posts: [Post!]!
}

type Mutation {
  publishPost(title: String!, body: String!): Post!
}
